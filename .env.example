# Required runtime storage backend.
# The API fails fast when this URL is not provided.
RVE_REDIS_URL=redis://127.0.0.1:6379

# Optional key prefix (works for Redis and Dragonfly).
RVE_REDIS_PREFIX=rve

# Optional logging baseline (CLI -v/-vv/-vvv still applies).
RUST_LOG=info
